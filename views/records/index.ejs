<%- include("./partials/header") %>

<div class="container" style="margin-top: 40px;">
  <h1>Car Maintenance Log</h1>
  <p>Track your oil changes, repairs, and vehicle maintenance history easily.</p>

  <% if (!currentUser) { %>
    <a class="btn btn-primary" href="/auth/google">Sign in with Google</a>
    <a class="btn btn-dark" href="/auth/github">Sign in with GitHub</a>
  <% } else { %>
    <a class="btn btn-success" href="/records">View My Records</a>
  <% } %>
</div>

<%- include("./partials/footer") %>
