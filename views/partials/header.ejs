<!DOCTYPE html>
<html>
<head>
  <title>Car Maintenance Log</title>

  <!-- Bootstrap 5 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">

  <!-- Your custom CSS -->
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/">Car Maintenance Log</a>

    <div>
      <% if (currentUser) { %>
        <a class="btn btn-outline-light" href="/records">My Records</a>
        <a class="btn btn-danger" href="/logout">Logout</a>
      <% } else { %>
        <a class="btn btn-primary" href="/auth/google">Sign in with Google</a>
        <a class="btn btn-secondary" href="/auth/github">Sign in with GitHub</a>
      <% } %>
    </div>
  </div>
</nav>
