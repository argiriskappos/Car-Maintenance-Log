<nav class="navbar ...">
  <div class="container">
    <a class="navbar-brand" href="/">Car Maintenance Log</a>
    <div>
      <a class="btn btn-outline-primary me-2" href="/records">View Records</a>

      <% if (!currentUser) { %>
        <a href="/auth/google" class="btn btn-light me-2">Sign in with Google</a>
        <a href="/auth/github" class="btn btn-dark">Sign in with GitHub</a>
      <% } else { %>
        <span class="me-2">Hello, <%= currentUser.displayName || currentUser.email %></span>
        <a class="btn btn-primary me-2" href="/records/new">Add Record</a>
        <a href="/auth/logout" class="btn btn-outline-danger">Logout</a>
      <% } %>
    </div>
  </div>
</nav>
